# Audio Switcher — 统一方案

> 单文档覆盖设计、技术与实现。

---

## 1. 概述

单文件托盘工具，切换播放设备、调节音量、反馈状态。常驻托盘，无主窗口。纯 Rust，Windows 11。

---

## 2. 功能设计

### 2.1 主菜单

分组顺序：播放设备 → 音量控制（静音、上限、实验性）→ 系统入口（合成器、声音设置）→ 应用（自启、Language、帮助、关于）→ 退出。左键无响应。

```
┌─ Audio Switcher ──────────────────┐
│ ●  扬声器 (Realtek)                │
│ ○  耳机 (USB Audio)                │
│ ───────────────────────────────── │
│ ✓  全局静音                        │
│    音量上限                   ▶  │
│    实验性功能                   ▶  │
│ ───────────────────────────────── │
│    打开音量合成器                  │
│    打开声音设置                    │
│ ───────────────────────────────── │
│ ✓  开机自启                        │
│    Language                     ▶  │
│    帮助                            │
│    关于                            │
│ ───────────────────────────────── │
│    退出                            │
└──────────────────────────────────┘
```

### 2.2 子菜单

**音量上限**
```
┌──────────────────┐
│ ✓  启用           │
│ ─────────────── │
│ ●  25%           │
│ ○  50%           │
│    自定义...     │
└──────────────────┘
```
启用默认勾选；禁用时阈值灰显。阈值默认 25%，自定义 1–100 整数。

**实验性功能**
```
┌──────────────────┐
│ ✓  滚轮加速       │
│    详细日志       │
└──────────────────┘
```
滚轮加速默认勾选，关闭后固定 1%/格；详细日志默认未勾选，勾选后写 `%TEMP%/audio-switcher-rust.log`。

**语言**
```
┌──────────────────┐
│ ●  中文           │
│ ○  English        │
└──────────────────┘
```
默认中文，父菜单固定 `Language`，切换即时生效。
### 2.3 触发

- 右键：弹出菜单
- 中键：切换静音，图标同步
- 悬停：`设备名 - 62%` / `静音`，超长截断
- 滚轮：悬停时调音量，200ms 窗口内 ≥3格 2%，≥5格或 <80ms 5%，`delta/120` 叠加，上限 5%/格

### 2.4 系统行为

托盘常驻、深浅色跟随系统、多屏适配、单实例、音量上限（启动/插拔/切换时钳制）。

### 2.5 默认值

| 配置 | 默认 |
|---|---|
| 开机自启 | 开启 |
| 音量上限 | 启用 25% |
| 滚轮加速 | 启用 |
| 详细日志 | 关闭 |
| 语言 | 中文 |
### 2.6 不做

气泡通知、分应用静音、热键、滑杆弹窗、版本号。

---

## 3. 技术方案

### 3.1 选型

| 层 | 技术 | 版本 |
|---|---|---|
| 语言 | Rust | 1.98 |
| 系统接口 | windows (Core Audio) | 0.62.2 |
| 托盘 | tray-icon / muda | 0.24.2 / 0.19.3 |
| 自启 | auto-launch | 0.6.0 |
| 单实例 | single-instance | 0.3.3 |
| 目标 | Windows 11 | - |

原则：优先复用成熟 crate，无则手写。无前端，无 WebView2。

### 3.2 模块

```
src/
  main.rs     // COM 初始化、消息循环
  audio.rs    // 枚举、切换、音量、监听
  tray.rs     // 图标、菜单、悬停、中键、滚轮、Language、i18n
  config.rs   // 持久化 %APPDATA%/AudioSwitcher/config.json（含 lang、version）
  system.rs   // 单实例 + 自启
```
约束：`main` 统一 `CoInitializeEx(STA)`；`audio` 与 `tray` 同线程；COM 及时 Release；`audio` 800ms 缓存。

### 3.3 实现

| 功能 | 方式 |
|---|---|
| COM | `CoInitializeEx(STA)`，失败退出 |
| 枚举 | `IMMDeviceEnumerator::EnumAudioEndpoints(Render, Active)`，800ms 缓存，空列表保持当前 |
| 切换 | `IPolicyConfig::SetDefaultEndpoint` 动态加载，失败 `MessageBox` 提示并保持 |
| 监听 | `IMMNotificationClient`，插拔时清缓存 |
| 音量/静音 | `IAudioEndpointVolume`，失败 `MessageBox` 提示并重试一次 |
| 上限 | 启动/切换/插拔时钳制 |
| 自定义 | 校验 1–100 整数，非法 `MessageBox` 提示 |
| 滚轮 | `WM_MOUSEWHEEL` 200ms 窗口 |
| 中键 | `WM_MBUTTONDOWN` 切换静音 |
| 悬停 | `NIM_MODIFY` 更新 tooltip |
| 菜单 | `muda` HMENU，语言切换重建 |
| 语言 | `config.lang` + `tray::i18n` |
| 图标 | `WM_SETTINGCHANGE` 切换深浅 |
| 日志 | 实验性开关控制，开启写 `%TEMP%/audio-switcher-rust.log` |
| 系统入口 | `ShellExecuteW`，失败 `MessageBox` 提示 |
| 配置 | `serde_json`，`version` 兼容旧文件 |

### 3.4 风险

| 风险 | 对策 |
|---|---|
| `IPolicyConfig` 未公开 | 动态加载，失败 `MessageBox` 提示 |
| COM 线程 | 统一 STA |
| 插拔并发 | 防抖，清缓存 |
| 钳制回写 | 屏蔽监听 |
| 多屏/DPI | `PerMonitorV2` |
| 配置损坏/迁移 | 回默认值，`version` 兼容 |
| 主题切换 | 重载图标 |
| 日志 | 默认关闭，不影响性能 |

构建：`cargo build --release` 单文件。

---

## 4. 实现计划

| 阶段 | 目标 |
|---|---|
| P0 | 底座：工程、依赖、manifest、图标 |
| P1 | 音频：枚举、切换、音量、监听 |
| P2 | 托盘：菜单、悬停、中键、滚轮、Language |
| P3 | 系统：`system.rs` 单实例/自启、配置、入口、对话框 |
| P4 | 收尾：联调、边界、构建 |

验收：菜单分组正确；切换、静音、滚轮、悬停、语言实时生效；插拔/重启上限与语言持久化；单实例、自启、多屏正常。

---

## 5. 测试方案

自动化为主，Vibe Coding 一次性交付。

| 类型 | 方式 | 覆盖 |
|---|---|---|
| 单元 | `cargo test` | `config`、`tray::i18n`、钳制、滚轮加速 |
| 集成 | `cargo test -- --ignored` | `audio` 经 `trait AudioBackend` mock |
| 冒烟 | `pwsh scripts/smoke.ps1` | 启动、切换、静音、滚轮、语言、自启、DPI/主题 |
| 静态 | CI | `clippy` 零警告、`fmt`、`build` |

Vibe 补齐：
- `audio.rs` 抽象 `trait AudioBackend`，`MockBackend` 隔离
- `config` 测试用 `tempfile` 隔离
- 提示词覆盖默认值、边界 1/100、语言切换
- CI 固定 `pwsh 7`，`cargo test` 阻塞发布
